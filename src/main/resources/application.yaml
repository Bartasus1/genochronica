spring:
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyComponentPathImpl
  datasource:
    url: jdbc://postgres:5432/mydatabase
    username: myuser
    password: secret
  application:
    name: genochronica
server:
  port: 8080
